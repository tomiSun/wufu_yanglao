import React, { useEffect } from 'react';
import { Button, Card, Col, Form, List, Row, Select, Tag } from 'antd';


const A = (props) => {
    return (
        <div>合同</div>
    )
};

export default A